------Dependencias------

Intalar Node.js

En la carpeta del archivo
npm init -y
npm install express
npm install sqlite3
npm install express-session
npm install multer


------Web------

arrancar el server a mano: node js/server.js
http://localhost:3000/

------Ejecutable------

Instalar thunder client en extensiones VsCode

(npm install --save-dev electron@32)
(npm install --save-dev @electron-forge/cli @electron-forge/maker-zip)

-hacer ejecutable-

npm isntall
npm run make  
npm start

out/
  TuApp-darwin-x64/
  make/
    zip/
      darwin/
        TuApp-darwin-x64-<version>.zip

Ese .zip contendrá la aplicación de escritorio.


---Cambiar contraseña admin---
En server.js añadir

const nuevaPassword = await hashPassword("tu_nueva_password");

await run(
  `UPDATE users SET password = ? WHERE LOWER(name) = 'admin' OR LOWER(email) = 'admin@local'`,
  [nuevaPassword]
);

console.log("Contraseña del admin actualizada");

ejecutar el server y luego borrar este código(o no)

---Añadir vacaciones---
Añadir
node -e "const sqlite3=require('sqlite3').verbose();const db=new sqlite3.Database('reservas.db');db.run('INSERT INTO holidays (date, description) VALUES (?, ?)', ['2026-05-25','Festivo local'], function(err){ if(err){console.error(err.message)} else {console.log('Festivo añadido')} db.close();});"	
Borrar
node -e "const sqlite3=require('sqlite3').verbose();const db=new sqlite3.Database('reservas.db');db.run('DELETE FROM holidays WHERE date = ?', ['2026-05-25'], function(err){ if(err){console.error(err.message)} else {console.log('Festivo eliminado')} db.close();});"
Ver
node -e "const sqlite3=require('sqlite3').verbose();const db=new sqlite3.Database('reservas.db');db.all('SELECT date, description FROM holidays ORDER BY date', [], (err, rows) => { if(err){console.error(err.message)} else {console.table(rows)} db.close();});"









---YA NO USAR---


Instalar thunder client en extensiones VsCode

--> Registros <--

Para meter usuario,entrar en thunder client
New Request
Seleccionar POST (desplegable a la izquierda del url)
Body
Json 
Pegar en la url http://localhost:3000/api/auth/register
Pegar en el json (EJEMPLO):
    {
        "name": "Juan",
        "email": "juan@correo.com",
        "password": "123456"
    }
send

--> Login <--

Para loguear, entrar en thunder client
new request 
Seleccionar Post
Body
Json
Pegar en la url http://localhost:3000/api/auth/login
Pegar en el json (Siguiendo el ejemplo anterior):
    {
        "identifier": "juan@correo.com",
        "password": "123456"
    }
send

--> Log Out <--

thunder client
new request
Post
http://localhost:3000/api/auth/logout
send
(O INICIAR SESION CON OTRO USUARIO)

--> Ver Usuario Logueado<--

thunder client
new request 
get
http://localhost:3000/api/auth/me
send


ACTUALMENTE login pulsando F12 en la web y escribiendo en la consola 
fetch("/api/auth/login", {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    identifier: "admin",
    password: "admin"
  })
})
  .then(res => res.json())
  .then(data => console.log(data));
